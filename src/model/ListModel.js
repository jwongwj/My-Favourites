export default class ListModel {
  constructor(name, url, description, checked, files) {
    this.name = name;
    this.url = url;
    this.description = description;
    this.checked = checked;
    this.files = files;
  }
}
